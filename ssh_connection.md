# SSH Connection

Assuming `avahi-daemon` and `avahi-utils` are pre-installed with Jetson Linux:

1. Connect Ethernet cable to the kit

2. Open a SSH session from another device:

> ssh username@ubuntu.local

Remarks: Replace `username` with your username.